// import { RouterProvider } from "react-router-dom";
// import "./App.css";
// import routers from "./pages/routers";
// import useLoadPage from "./hooks/useLoadPage";

function App() {
  // const { isLoaded, isSignedIn } = useLoadPage();
  // return isLoaded ? (
  //   <RouterProvider router={isSignedIn ? routers.private : routers.public} />
  // ) : null;
}

export default App;
